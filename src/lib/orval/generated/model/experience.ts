/**
 * Generated by orval v7.13.1 🍺
 * Do not edit manually.
 * Gradegy API
 * Interactive documentation for the Gradegy API
 * OpenAPI spec version: 1.0
 */
import type { ExperienceTimingType } from './experienceTimingType';
import type { ExperienceCompletionType } from './experienceCompletionType';

export interface Experience {
  /** Title displayed to learners for the experience. */
  title: string;
  /** Short supporting headline that clarifies the experience. */
  subtitle?: string;
  /** Full description of the experience, including requirements. */
  description: string;
  /** Primary image or illustration for the experience. */
  image?: string;
  /** Identifier of the experience type that governs styling. */
  experience_type: string;
  /** Organization whose members can access the experience. */
  organization: string;
  /** Optional project scoping this experience to a project audience. */
  project?: string;
  /** Optional category that narrows visibility for the experience. */
  category?: string;
  /** Optional subcategory audience constraint. */
  subcategory?: string;
  /** Determines how the experience availability window is calculated. */
  timing_type: ExperienceTimingType;
  /**
   * Number of whole days to delay before showing the experience when timing_type is delay_after_previous.
   * @minimum 0
   */
  delay_days?: number;
  /**
   * Inclusive number of days the experience remains available for start_date_and_length timing.
   * @minimum 1
   */
  length_days?: number;
  /** Experience that must be satisfied before this one becomes available. When set the experience is considered a child. */
  prerequisite?: string;
  /** Defines the ordering of experiences within a program. Lower numbers appear earlier. */
  sequence?: number;
  /** Require learners to complete the experience before the next delay can begin. */
  completion_required?: boolean;
  /** When true, a delayed child experience tied to a dated parent ends when the parent ends. */
  end_with_parent?: boolean;
  /** Calendar date when the experience becomes visible to learners. */
  start_date?: string;
  /** 24-hour time when the experience unlocks on the start date. */
  start_time?: string;
  /** Calendar date when the experience expires for learners. */
  end_date?: string;
  /** 24-hour time on the end date when the experience moves to past due. */
  end_time?: string;
  /**
   * XP awarded when the learner submits the required evidence.
   * @minimum 0
   */
  xp_completion: number;
  /**
   * XP awarded when the learner first views the experience.
   * @minimum 0
   */
  xp_view: number;
  /**
   * Gems granted upon completing the experience.
   * @minimum 0
   */
  gems: number;
  /** How the learner confirms completion of the experience. */
  completion_type: ExperienceCompletionType;
  /** URL learners must visit to satisfy completion when completion_type is link. */
  complete_url?: string;
  /** Automatically credit XPs and gems upon learner submission when true. */
  auto_complete: boolean;
  /** Optional call-to-action label displayed beside the link URL. */
  link_text?: string;
  /** Optional informational link that accompanies the experience. */
  link_url?: string;
}
