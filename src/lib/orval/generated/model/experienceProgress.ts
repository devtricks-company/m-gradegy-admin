/**
 * Generated by orval v7.13.1 🍺
 * Do not edit manually.
 * Gradegy API
 * Interactive documentation for the Gradegy API
 * OpenAPI spec version: 1.0
 */
import type { ExperienceProgressStatus } from './experienceProgressStatus';
import type { ExperienceCompletionType } from './experienceCompletionType';
import type { ExperienceEvidence } from './experienceEvidence';
import type { Map } from './map';

export interface ExperienceProgress {
  /** Experience that this progress entry tracks. */
  experience: string;
  /** Learner associated with the tracked experience. */
  user: string;
  /** Owning organization scoped from the experience. */
  organization: string;
  /** Project copied from the experience scope. */
  project?: string;
  /** Category copied from the experience scope. */
  category?: string;
  /** Subcategory copied from the experience scope. */
  subcategory?: string;
  /** Current stage of the learner progress. */
  status: ExperienceProgressStatus;
  /** Snapshot of completion type at the time of creation. */
  completionTypeSnapshot: ExperienceCompletionType;
  /** True when the experience was configured for auto completion. */
  autoComplete: boolean;
  /**
   * XP amount actually awarded to the learner.
   * @minimum 0
   */
  xpAwarded: number;
  /**
   * Gems amount actually awarded to the learner.
   * @minimum 0
   */
  gemsAwarded: number;
  /** Date when the learner first viewed the experience. */
  viewedAt?: string;
  /** Date when the learner submitted completion evidence. */
  submittedAt?: string;
  /** Date when progress moved to completed. */
  completedAt?: string;
  /** Date when a submission was rejected by an administrator. */
  rejectedAt?: string;
  /** Administrator who reviewed the submission. */
  adminReviewer?: string;
  /** Optional note left by the reviewer. */
  reviewNote?: string;
  /** Evidence payload supplied by the learner. */
  evidence?: ExperienceEvidence;
  /** Flexible metadata for specialized completion payloads. */
  metadata?: Map;
}
